import en from './en/condition.distance.json';
import ru from './ru/condition.distance.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
