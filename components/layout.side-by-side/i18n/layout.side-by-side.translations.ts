import en from './en/layout.side-by-side.json';
import ru from './ru/layout.side-by-side.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
