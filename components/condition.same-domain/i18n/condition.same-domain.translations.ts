import en from './en/condition.same-domain.json';
import ru from './ru/condition.same-domain.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
