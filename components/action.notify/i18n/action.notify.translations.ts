import en from './en/action.notify.json';
import ru from './ru/action.notify.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
