import en from './en/field.phone-number.json';
import ru from './ru/field.phone-number.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
