import en from './en/field.image-annotation.json';
import ru from './ru/field.image-annotation.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
