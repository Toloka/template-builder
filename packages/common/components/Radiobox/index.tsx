import {
    Radiobox as LegoRadiobox,
    RadioboxProps as IRadioboxProps,
} from '@yandex/ui/Radiobox/desktop/bundle';

export const Radiobox = LegoRadiobox;

export type RadioboxProps = IRadioboxProps;
