import { EmbedFeatures } from '@toloka-tb/iframe-api/launcher.embed/protocol';

export const defaultFeatures: EmbedFeatures = {
    submit: undefined,
    locales: ['en', 'ru'],
    submitByEnter: true
};
