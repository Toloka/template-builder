import en from './en/launcher.embed.json';
import ru from './ru/launcher.embed.json';

const translations = {
    en: {
        ...en
    },
    ru: {
        ...ru
    }
};

export default translations;
